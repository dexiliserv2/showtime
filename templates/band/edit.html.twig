{% extends 'base.html.twig' %}

{% block title %}Edit Band{% endblock %}

{% block body %}
    <style>
        h1 {
            padding-left: 15px;
            padding-top: 10px;
        }

        div {
            padding-left: 15px;
            padding-top: 5px;
        }
    </style>
    <h1 class="text-2xl">Edit Band</h1>

    <div>
        {{ include('band/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
    <div>
        <button type="button" class="btn" onclick="window.location.href='{{ path('app_band_index') }}';">
            Back to list
        </button>
    </div>
    <div>
        {{ include('band/_delete_form.html.twig') }}
    </div>
{% endblock %}
